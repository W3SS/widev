<%= bootstrap_form_for(@rel_template) do |f| %>
  <% if @rel_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rel_template.errors.count, "error") %> prohibited this rel_template from being saved:</h2>

      <ul>
      <% @rel_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :name %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
