<%= form_for(@check_result) do |f| %>
  <% if @check_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@check_result.errors.count, "error") %> prohibited this check_result from being saved:</h2>

      <ul>
      <% @check_result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_area :value %>
  </div>
  <div class="field">
    <%= f.label :query_check_id %><br>
    <%= f.text_field :query_check_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
